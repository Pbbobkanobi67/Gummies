{
  "name": "blue-raffle-blockhash",
  "version": "2.0.0",
  "description": "Modular blockchain raffle using blockhash randomness",
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network bsc_testnet",
    "deploy:local": "hardhat run scripts/deploy.js --network hardhat",
    "test": "hardhat test",
    "node": "hardhat node",
    "clean": "hardhat clean"
  },
  "keywords": [
    "raffle",
    "blockchain",
    "hardhat",
    "solidity",
    "bsc"
  ],
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.5",
    "@nomicfoundation/hardhat-verify": "^2.0.3",
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.3.1",
    "ethers": "^6.10.0",
    "hardhat": "^2.19.4"
  }
}
